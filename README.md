# Fallstudie-Semester-3
Emotion &amp; Age Detection

requirements:
Python packages:
pip install streamlit
pip install streamlit-option-menu
pip install opencv-python
pip install Pillow
pip install numpy
pip install deepface
pip install pandas


